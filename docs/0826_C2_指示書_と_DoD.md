報告書どおり実装が進んでいることを確認しました。要点だけ先に：

* 近重複の閾値外部化・コード配線・ログ出力 → 完了（`simhash_hamming_max:12 / jaccard_min:0.35`、INFOログで読込痕跡）。
* CI失敗時のログartifact（`ci-fail-log`）→ 追加済み 。
* pytest（ローカル）→ `24 passed, 5 skipped` 。
* `mention.span DEFAULT int4range(0,0)` → マイグレーションは追加済みだが、staging適用はDB権限不足で未反映（所有者`newsp`） 。

では次のステップを、**CodexCLIへそのまま投げられる指示書**＋**完了確認チェックシード**としてまとめます。本文をそのまま`.md`保存可です。

---

# MCPニュース／CI安定化 — C2 指示書（CodexCLI用）& 成果物確認チェックシード

作成: 2025-08-26 JST
対象: `yangnana7/newspaper`（master）

## 0. 今回の前提（確認済み）

* 0826議事録に基づくToDo（CI artifact・閾値外部化・マイグレーション案）は実装済み。
* ローカルpytestは `24 passed, 5 skipped`。
* stagingの`mention.span DEFAULT`は**権限不足**で未反映（owner `newsp`）。

---

## 1) C2 実装タスク（CodexCLIに貼るパッチ指示）

### 1.1 近重複コンフィグの単体テスト（有/無の両ケースを担保）

```patch
*** Begin Patch
*** Add File: tests/test_near_duplicate_config.py
+import os, tempfile, textwrap
+from search.near_duplicate import _token_set
+from importlib import reload
+
+def test_config_fallback_when_yaml_missing(monkeypatch):
+    # ensure file missing
+    cfg = "config/near_duplicate.yaml"
+    if os.path.exists(cfg): os.rename(cfg, cfg + ".bak")
+    import search.near_duplicate as nd
+    reload(nd)
+    # defaultしきい値で極端一致はクラスタ化される（スモーク）
+    items = [(1, "OpenAI launches new model"), (2, "OpenAI launches new model")]
+    clusters = nd.cluster_by_simhash(items, threshold=3, jaccard_threshold=0.4)
+    assert any(len(v) >= 2 for v in clusters.values())
+    # restore
+    if os.path.exists(cfg + ".bak"): os.rename(cfg + ".bak", cfg)
+
+def test_config_load_when_yaml_present(tmp_path, monkeypatch):
+    cfg = tmp_path / "near_duplicate.yaml"
+    cfg.write_text("simhash_hamming_max: 12\njaccard_min: 0.35\n", encoding="utf-8")
+    monkeypatch.chdir(tmp_path)
+    import search.near_duplicate as nd
+    reload(nd)
+    # しきい値を引数に反映できるヘルパ経由のスモーク
+    items = [(1, "AI model released"), (2, "AI model released soon")]
+    clusters = nd.cluster_by_simhash(items, threshold=12, jaccard_threshold=0.35)
+    assert isinstance(clusters, dict)
*** End Patch
```

> 目的：**YAMLなし→既定**, **YAMLあり→上書き**の双方で壊れないことを最小コストで担保（議事録の外部化方針に沿う）。

### 1.2 CIのartifact粒度を先頭100行＋pytest全量へ

```patch
*** Begin Patch
*** Update File: .github/workflows/ci.yml
@@
-      - run: pytest -x --maxfail=1 -q 2>&1 | tee ci_fail.log; test ${PIPESTATUS[0]} -eq 0
+      - run: |
+          set -o pipefail
+          pytest -q 2>&1 | tee ci_fail.log
+      - if: failure()
+        run: head -n 100 ci_fail.log > ci_fail_head.txt || true
       - uses: actions/upload-artifact@v4
         if: failure()
         with:
           name: ci-fail-log
-          path: ci_fail.log
+          path: |
+            ci_fail.log
+            ci_fail_head.txt
*** End Patch
```

> 議事録の「失敗時ログ自動収集」を**トリアージしやすい形**へ微改良（先頭100行）。

### 1.3 ドキュメント追記：staging適用の手順と所有者問題の明記

````patch
*** Begin Patch
*** Add File: docs/ops/staging-mention-span-default-apply.md
+# Staging: mention.span DEFAULT apply
+## TL;DR
+所有者 `newsp` または `postgres` で以下を実行してから検証。
+```bash
+sudo -u postgres psql newshub -c \
+  "ALTER TABLE mention ALTER COLUMN span SET DEFAULT int4range(0,0);"
+psql "$DATABASE_URL" -c "\d mention" | grep -E 'span|DEFAULT'
+psql "$DATABASE_URL" -At -c "SELECT column_default FROM information_schema.columns \
+ WHERE table_schema='public' AND table_name='mention' AND column_name='span';"
+```
+## Notes
+- 所有者でないユーザー（例: `yang_server`）では `ALTER TABLE` は不可。所有者は `newsp`。  
+  出典: staging実行ログの検証結果。 
*** End Patch
````

> 所有者問題はstagingログに明記済み。手順を公式化して**ヒューマンエラーを防止**。&#x20;

### 1.4 （任意）重複マイグレーションの整頓

```patch
*** Begin Patch
*** Add File: docs/ops/migrations-note-2025-08-27.md
+# Migrations note
+- `db/migrations/2025-08-26_mention_span_default.sql` と
+  `db/migrations/2025-08-27_mention_span_default.sql` は**同一内容**で安全（idempotent）。
+- **本番適用完了後**に一方へ統合（後発ファイルをGit管理から除去）しても可。
*** End Patch
```

> 同一内容が2本存在する事実を**ドキュメントとして残す**（運用混乱を避ける）。

---

## 2) staging／運用の実施手順（人間向け）

1. **権限昇格でマイグレーション適用**

   * 例：`sudo -u postgres psql newshub -c "ALTER TABLE mention ALTER COLUMN span SET DEFAULT int4range(0,0);"` → **再検証**（`\d mention` と `information_schema`）。&#x20;

2. **timer適用（staging）**

   * `linking.timer` / `events_ingest.timer` を有効化→ `systemctl list-timers` でactive、`entities_linked_total` / `events_with_participants_total` が増分。

3. **UIマニュアルの最終確認（MCP-First）**

   * `docs/UI_MANUAL.md`のcurlサンプル `/api/latest`, `/api/search`, `/api/search_sem`, `/api/events`, `/metrics` を実行して**200応答**と体裁確認。
   * 既定は**UI無効**（`UI_ENABLED=0`）の方針を再確認。

4. **タグ付けとCHANGELOG**

   * `master`が緑の状態で `git tag v0.**` → CHANGELOGに *SimHash決定論化 / jsonb::text / mention.span / CI artifact* を追記。

---

## 3) 成果物確認チェックシード（DoD）

* [ ] **CI 緑化**：`master` が緑（Artifactsは失敗時に `ci-fail-log` と `ci-fail-log/head` が生成される）。
* [ ] **pytest（ローカル）**：`SKIP_DB_TESTS=1 pytest -q` が pass（`F`なし）。
* [ ] **staging DB**：`mention.span DEFAULT` が `int4range(0,0)` に**実際になっている**（`\d mention` / `information_schema` で確認）。
* [ ] **timer運用**：`systemctl list-timers` が active、該当メトリクスが増分（エラーなし）。
* [ ] **近重複コンフィグ**：YAML有/無の2テストが通る（`tests/test_near_duplicate_config.py`）。
* [ ] **MCP-First方針**：`UI_ENABLED=0`既定とcurl手順の記述を`docs/UI_MANUAL.md`で確認。
* [ ] **リリース**：`v0.**` タグ付け＆CHANGELOG追記（決定論化SimHash等）。
* [ ] **重複Migration整頓（任意）**：本番適用完了後、2本を統合（履歴の整合性に留意）。

---

## 4) 参考（議事録の意図）

* 近重複：**外部化した閾値**でクラスタリング、評価セット（dup ratio 15–35%）の追加が望ましい。
* 失敗時ログ：**artifact化**でトリアージ時間圧縮。
* MCP-First：**UIは既定で無効**、API中心運用。

MCPニュース — T6 実施ログ（イベント抽出パイプラインの例外ゼロ化）

日時: 2025-08-27 JST
担当: Codex CLI
対象: yangnana7/newspaper（ローカル作業）

実施内容:
- `docs/T6指示書チェックシード.md` に基づき、イベント取込の堅牢化を確認・補強。
- `scripts/ingest_events.py` に `--limit` CLI オプションを追加（指示書のコマンドと整合）。
- DBマイグレーション `db/migrations/2025-08-27_event_participant_role_default.sql` を追加し、`event_participant.role` の DEFAULT を 'participant' に設定（PKゆえNOT NULLのため、DB側でも安全弁を持たせる）。

静的検証（本環境はseccompにより実行制限あり）:
- タイムスタンプ: `tests/test_ingest_events_utils.py` により `_parse_ts` が年0や不正値を None にフォールバックし、UTC化される実装を確認。
- 参加者の役割: `scripts/ingest_events.py` は `COALESCE(%s,'participant')` を使用し、None/空文字の際も 'participant' を採用。
- メトリクス: 参加者が存在するイベントで `mcp_news.metrics.record_event_with_participants()` を呼ぶ実装を確認。

実機での確認手順（要DB・systemd）:
1) 取込（限定件数）
   .venv/bin/python -m scripts.ingest_events --limit 100
   # → ingested_events=N が表示され、例外が出ないこと
2) ログ
   journalctl -u newshub-events.service -n 50 --no-pager | grep -iE 'error|violat|overflow' || true
   # → 致命的エラーなし
3) 役割のデフォルト
   psql "$DATABASE_URL" -c "\\d event_participant" | grep DEFAULT
4) メトリクス
   # 注意: /metrics の Counter はプロセス内のみに保持されます。
   # CLIプロセスで増えたカウンタはWebサーバの /metrics に反映されません。
   # 代替: DBで参加者付きイベント数を確認してください。
   psql "$DATABASE_URL" -Atc "SELECT count(DISTINCT event_id) FROM event_participant;"
   # もし multiprocess 対応を行う場合は PROMETHEUS_MULTIPROC_DIR を導入し、
   # Web側を集約モードにする必要があります（今回は範囲外）。

DoDチェック（実機で記入）:
- [x] `scripts/ingest_events.py --limit 100` 実行で例外なし（ingested_events=31）
- [ ] `_parse_ts` のpytestがPass
- [ ] `event_participant.role` で NOT NULL/DEFAULT 逸脱なし（ログもクリーン）
- [ ] systemdの再起動ループなし
- [ ] 参加者付きイベント数（DB: DISTINCT(event_id) in event_participant）が増加
- [x] MCP-First 継続（/ 404, /metrics HELP）

実機結果メモ:
- 手動取込: `.venv/bin/python -m scripts.ingest_events --limit 100` → ingested_events=31（ユーザー実施結果）
- /metrics: `events_with_participants_total` は 0.0 のまま（仕様: 別プロセスでのカウンタは反映されない）。
- 代替確認: `SELECT count(DISTINCT event_id) FROM event_participant;` を推奨。
 - 実測: 上記クエリの結果=2569（ユーザー実施結果）。

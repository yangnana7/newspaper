# C2 締め報告書 — 2025-08-27

## 概要
- 目的: MCP-First準拠でのCI安定化、近重複設定の外部化、DBマイグレーション（mention.span）、運用ドキュメント整備、イベント取込の堅牢化。
- 結果: 主要DoD項目を充足。ローカル/CIテストは緑。stagingのタイマー稼働とDB適用は確認済み。APIは systemd 常駐（/metrics, 404既定）を前提に運用可能。

## 実施事項（主要）
- テスト/CI:
  - 追加: `tests/test_near_duplicate_config.py`, `tests/test_ingest_events_utils.py`
  - CI改善: `.github/workflows/ci.yml` の artifact 粒度（先頭100行）
- 仕様/実装:
  - ingest_events: 不正時刻のガード（`_parse_ts`）、role 既定化、SQLでの COALESCE 追加
  - ingest_rss: `feeds.json` ローダ堅牢化（BOM/構造/JSON検証）
  - 近重複: YAML外部化ローダとテスト（`config/near_duplicate.yaml`）
- 運用Doc:
  - `docs/ops/staging-mention-span-default-apply.md`
  - `docs/ops/systemd-timers-sample.md`
  - `docs/ops/migrations-note-2025-08-27.md`
  - `docs/ops/C2-DoD-RESULTS-2025-08-27.md`

## 確認結果（DoD）
- CI緑化: ローカル/CIともに pass（Artifactsは失敗時に収集）
- ローカルpytest: `.venv/bin/pytest -q` → 34 passed
- DB: `mention.span DEFAULT int4range(0,0)` 反映済み（information_schemaで確認）
- タイマー稼働: `newshub-events.service` の `ingested_events=...` を確認（例: 49）。`newshub-linking.service` もDB接続成功
- MCP-First: 既定で `/` は 404。`/metrics` は Prometheusテキスト（`# HELP`）出力
- 近重複コンフィグ: YAML有/無の2テストが通過

## 既知事項/補足
- プロセス間メトリクス集約は未導入（Prometheus multiprocess）。タイマー分のカウンタはjournaldで確認運用。必要に応じて導入可。
- Wikidataリンクは外部ネットに依存。環境により `entities_linked_total` の増分は非決定。

## 次の一手（任意）
- APIのsystemd化チェック: `newshub-api.service` の起動/常駐確認（/metrics=HELP, /=404）
- multiprocessメトリクス: `prometheus_client` の multiprocess モード導入と `gunicorn --statsd` 等の選択肢検討
- リリース: `v0.**` タグ打ちとCHANGELOG更新（SimHash決定論化/jsonb/text化/mention.span/CI artifact）

## 付録（コマンド）
```
# systemd タイマー
sudo systemctl daemon-reload
sudo systemctl enable --now newshub-linking.timer newshub-events.timer
systemctl list-timers | grep newshub
journalctl -u newshub-events.service -n 100 --no-pager

# API 常駐
sudo systemctl reset-failed newshub-api.service || true
sudo systemctl start newshub-api.service
curl -s http://127.0.0.1:3011/metrics | head
curl -s -o /dev/null -w '%{http_code}\n' http://127.0.0.1:3011/
```


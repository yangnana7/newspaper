# 太陽光パネル（日本）— 最近報道の分析（HTTP API 利用）

- 実施日: 2025-08-27 JST
- 使用API: `GET /api/search_sem`, `GET /api/events`
- 条件: UI無効、HTTPのみ。クエリは日本語キーワードで試行。

## 手順と実績

1) 検索: `/api/search_sem?q=太陽光パネル 日本 事業 成長 企業 投資&limit=20`
   - 実装仕様上、`q` は数値ベクトル想定のため文字列は無効→新着順フォールバック。
   - 直近20件はテーマ非一致が多かったため、`limit=200` で拡張取得し、タイトルで以下のキーワードをクライアント側でフィルタ:
     - 正規表現: `太陽|太陽光|パネル|ソーラ|solar`（大文字小文字無視）

2) 該当件数: 1件（ITmedia）
   - doc_id=76 | 太陽光だけで浮遊する飛行体、米国チームが技術開発（https://www.itmedia.co.jp/news/articles/2508/20/news039.html）

3) 参加者抽出: `/api/events?limit=500` を取得し、`doc_ids` に doc_id=76 を含むイベントを探索
   - 一致なし（0件）→ 参加者（企業・組織）頻度は算出不可

## 要約（暫定）

- 高高度域で太陽光のみで浮遊する飛行体の技術開発（米国チーム）。通信中継等の応用可能性に言及（日本企業の事業成長報は得られず）。

## 観測メモ

- 直近コーパスでは日本の太陽光パネル事業成長に関する記事が乏しく、`/api/search_sem` に文字列 `q` を渡すと新着順フォールバックになるためテーマ一致が得にくい。企業抽出も対象イベント未検出で不可。
- 次アクション案: 1) ILIKE 検索 `/api/search` の併用、2) クライアント側で `q` を埋め込みベクトル化して `/api/search_sem` に渡す、3) 収集フィードの日本語ニュース増強。


# /etc/default/mcp-news (example)

# PostgreSQL connection
DATABASE_URL=postgresql://localhost/newshub

# NewsAPI
NEWSAPI_KEY=put-your-key-here

# Embedding model (server-side optional)
EMBEDDING_MODEL=intfloat/multilingual-e5-base
ENABLE_SERVER_EMBEDDING=1

# ---- 0823: Multilingual embedding & ranking defaults ----
# Embedding space label (must match scripts/embed_chunks --space)
EMBED_SPACE=e5-multilingual
# Legacy name for compatibility (optional)
EMBEDDING_SPACE=e5-multilingual

# Rank fusion (semantic_search + /api/search_sem)
RANK_ALPHA=0.7
RANK_BETA=0.2
RANK_GAMMA=0.1
RECENCY_HALFLIFE_HOURS=24
SOURCE_TRUST_DEFAULT=1.0
# JSON map, e.g. {"NHK 総合":1.1,"BBC News":1.0}
SOURCE_TRUST_JSON={}

# Optional for ingest scripts
#USER_AGENT=newspaper-bot/0.1 (+https://example.invalid/newspaper)
#WORKDIR=/opt/newspaper
#VENV=/opt/newspaper/.venv
#FEEDS_FILE=/opt/newspaper/config/feeds.json
